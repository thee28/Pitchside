services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: estadio
      POSTGRES_PASSWORD: estadio
      POSTGRES_DB: estadio
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: