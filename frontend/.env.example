# Backend API origin for production builds served from a different origin than
# the API (e.g. https://api.pitchside.example). Leave unset in dev: calls stay
# relative and hit the Vite proxy in vite.config.js.
VITE_API_BASE=
